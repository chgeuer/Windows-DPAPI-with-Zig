[package]
name = "dpapi"
version = "0.1.0"
authors = []
edition = "2021"

[lib]
name = "dpapi"
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
rustler = "0.30.0"
winapi = { version = "0.3", features = ["winbase", "windef", "consoleapi"] }
